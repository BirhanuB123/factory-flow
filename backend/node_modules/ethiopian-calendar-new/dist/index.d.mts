/**
 * Checks if an Ethiopian year is a leap year.
 * Now specifically matches the 4-year cycle where:
 * - 2015 (2023 GC) was leap (Pagume 6 on Monday, Sept 11)
 * - Next is 2019 (2027 GC) will be leap (Pagume 6 on Saturday, Sept 11)
 * @param year Ethiopian year
 * @returns True if a leap year, false otherwise
 */
declare function isEthiopianLeapYear(year: number): boolean;
/**
 * Converts an Ethiopian date to Gregorian date.
 * @param ethYear Ethiopian year.
 * @param ethMonth Ethiopian month (1-13).
 * @param ethDay Ethiopian day (1-30, or 1-6 for Pagume).
 * @returns Gregorian { year, month, day }.
 */
declare function toGregorian(ethYear: number, ethMonth: number, ethDay: number): {
    year: number;
    month: number;
    day: number;
};
/**
 * Converts a Gregorian date to Ethiopian date.
 * @param gregYear Gregorian year.
 * @param gregMonth Gregorian month (1-12).
 * @param gregDay Gregorian day (1-31).
 * @returns Ethiopian { year, month, day }.
 */
declare function toEthiopian(gregYear: number, gregMonth: number, gregDay: number): {
    year: number;
    month: number;
    day: number;
};
declare const _default: {
    toGregorian: typeof toGregorian;
    toEthiopian: typeof toEthiopian;
    isEthiopianLeapYear: typeof isEthiopianLeapYear;
};

export { _default as default, isEthiopianLeapYear, toEthiopian, toGregorian };
